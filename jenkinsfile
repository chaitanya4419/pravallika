pipeline {
   agent any
   stages {
      stage('Build') {
         steps {
            // Get some code from a GitHub repository
            git 'https://github.com/chaitanya4419/pravallika.git'
	    sh 'uptime'
		 }
	 }
   }
}
